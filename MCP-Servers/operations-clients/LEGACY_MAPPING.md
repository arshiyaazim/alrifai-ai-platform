# Operations and Clients Legacy Mapping

Evidence: `modules/escort_lifecycle`, `escort_roster`, `client_billing`, dispatch routes; live `wbom_escort_programs` (347), `escort_roster_entries` (360), change requests, slip extractions, `ops_programs`, rates, `wbom_clients`, billing profiles/records (`VERIFIED_LIVE`/`VERIFIED_SOURCE`). Roster fields include mother/lighter vessel, escort, destination, shifts, release point, status, salary/conveyance, deductions, and net payable.

Gap: message path is stricter/draft-oriented while roster/forms permit manual adjustment; client/contact/billing records overlap and billing records are currently empty. Preserve release evidence and do not infer completion from text alone.
