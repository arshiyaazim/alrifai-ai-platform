# Platform and Admin Workflows

Message: admin command/draft → Conversations classification → trusted capability check → Platform approval/orchestration → owning domain service → shared audit/event → response.

Form: authenticated admin report/approval → request validation → same approval/orchestration service → domain operation where authorized → audit/result. Reports are derived from authoritative domain records; AI may summarize but cannot approve privileged actions.
